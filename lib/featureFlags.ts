// Flags de features temporalmente ocultas. Cambiar el valor acá y commitear
// a `dev` es el único paso para activar/ocultar — ver CLAUDE.md.
export const FEATURE_FLAGS = {
  proyectosStatusVisible: true,
};
